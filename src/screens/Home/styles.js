import { StyleSheet } from "react-native";
import { horizontalScale } from "../../scripts/responsive";
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#020817",
    paddingHorizontal: horizontalScale(25)
  }
})